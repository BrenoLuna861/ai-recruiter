spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

server:
  error:
    include-stacktrace: never
    include-message: never
    include-binding-errors: never

logging:
  level:
    com.airecruiter: WARN
    org.springframework: WARN
    root: ERROR
